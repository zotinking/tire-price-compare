# 타이어 가격 비교

국내 타이어 판매/장착 플랫폼별 가격, 배송비, 장착비, 수집 상태를 한 화면에서 비교하는 MVP 웹페이지입니다.

## 실행

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:4173`을 엽니다.

## GitHub Pages

정적 파일만으로 동작하므로 GitHub Pages에서 저장소 루트(`/`)를 배포하면 됩니다.
